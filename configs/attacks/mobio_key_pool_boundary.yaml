classification: exploratory independent study, not benchmark_cb reproduction
dataset: MOBIO selected still images, identity-disjoint 12-session protocol
embedding_dir: data/processed/embeddings/mobio/buffalo_l_yunet
results_dir: results/mobio_key_pool_boundary
template_dim: 128
protection:
  scheme: biohash
primary_analysis: key_pool_boundary
key_seed: 90431
set_seed: 90437
repeats_per_identity: 8
exposures: [1, 2, 5, 10]
conditions:
  - system_key_pool_1
  - system_key_pool_2
  - system_key_pool_5
  - system_key_pool_10
  - independent_unseen_keys
models:
  - mean_mlp
  - max_mlp
  - deepsets
amplification_threshold: 0.05
training:
  seeds: [67, 77, 87]
  epochs: 400
  patience: 60
  hidden_dim: 256
  learning_rate: 0.001
  weight_decay: 0.0001
  mse_weight: 0.1
  bootstrap_resamples: 2000
  bootstrap_confidence: 0.95