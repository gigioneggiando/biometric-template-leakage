classification: exploratory independent study, not benchmark_cb reproduction
dataset: MOBIO selected still images, identity-disjoint 12-session protocol
embedding_dir: data/processed/embeddings/mobio/buffalo_l_yunet
results_dir: results/mobio_same_image_fresh_control
template_dim: 128
protection:
  scheme: biohash
  haar_sign_corrected: true
primary_analysis: descriptive_control
control_name: same-image different-fresh-key control
control_model: deepsets
exposure_mode: same_image_different_keys
split_reassignment_seed: 90803
key_seed: 90821
set_seed: 90833
repeats_per_identity: 8
exposures: [1, 10]
conditions:
  - independent_unseen_keys
models:
  - deepsets
amplification_threshold: 0.05
training:
  seeds: [439, 443, 449]
  epochs: 400
  patience: 60
  hidden_dim: 256
  learning_rate: 0.001
  weight_decay: 0.0001
  mse_weight: 0.1
  bootstrap_resamples: 2000
  bootstrap_confidence: 0.95
