# Proposed experiment readiness

Implemented now: deterministic key generation, a BioHash reference interface, identity/key-disjoint synthetic sets, single-template MLP and masked permutation-invariant DeepSets extraction, embedding recovery/linkage/verification metrics, and run artifacts.

Pending before empirical claims: authorized multi-image face data, a documented ArcFace checkpoint/extraction pipeline, a verified benchmark_cb source/protocol, and multiple random seeds on the selected real-data protocol.
