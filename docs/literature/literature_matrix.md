# Literature matrix

The machine-readable audit is [literature_matrix.csv](literature_matrix.csv). Entries marked `unclear` are deliberate: primary-source verification did not establish that detail. They must not be treated as affirmative evidence.

The key distinction is between a classical template-inversion attacker, an exact protected-template reconstruction attacker, a linkability measurement, and this project's proposed identity-embedding extractor operating across independently keyed observations.
